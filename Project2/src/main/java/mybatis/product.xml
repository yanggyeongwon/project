<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="product.model.Product">

	<select id="GetTotalCount" resultType="int">
		select count(*) from product
		<if test="whatColumn=='name'">
			where name like #{keyword}
		</if>
		
		<if test="whatColumn=='contens'">
			where contens like #{keyword}
		</if>
	</select>
	
	<select id="GetDataList" resultType="MyProduct">
		select * from product
		<if test="whatColumn=='name'">
			where name like #{keyword}
		</if>
		
		<if test="whatColumn=='contens'">
			where contens like #{keyword}
		</if>
		order by num desc
	</select>
	
	<select id="GetData" resultType="MyProduct">
		select * from product
		where num=#{num}
	</select>
	
<!-- 	<insert id="InsertData">
		insert into products(num,name,company, image, stock, price, contents, point, inputdate) 
		values(seqprod.nextval,#{name},#{company},#{image},#{stock},#{price},#{contents},#{point},#{inputdate} )  
	</insert>
	
	
	<update id="UpdateData">
		update product set 
		name=#{name},company=#{company}, image=#{image}, stock=#{stock}, 
		price=#{price},contents=#{contents}, 
		point=#{point}, inputdate=#{inputdate} 
		where num=#{num}
	</update>
	
	<delete id="DeleteData">
		delete from product
		where num = #{num}
	</delete>
	
	<update id="UpdateStock">
		update product set
		stock = stock - #{stock}
		where num = #{num}
	</update> -->
	
</mapper>

















