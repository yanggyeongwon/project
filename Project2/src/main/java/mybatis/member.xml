<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member.model.Member">
	<insert id="InsertData">
		insert into member(mnum,mname,mid,mpw,mpw2,memail) 
		values(mseq.nextval,#{mname},#{mid},#{mpw},#{mpw2},#{memail})
	</insert>
	<select id="GetData" resultType="MyMember">
		select * from member
		where mid=#{mid}
	</select>

	<update id="UpdateData">
		update member set 
		mname=#{mname},mid=#{mid}, mpw=#{mpw} , mpw2=#{mpw2}, memail=#{memail}
		where mnum=#{mnum}
	</update>
<delete id="DeleteData">
	delete from member
	where mnum = #{mnum}
</delete> 

</mapper>












